const db = require('../models/movieModels');
var axios = require("axios");
const userListController = {};



// This makes a db query to the sql database and retrieves all data for the frontend
userListController.getSavedMovies = (req, res, next) => {
  // something
};



module.exports = userListController;
